# Manual task deletion and CLI health panel

## Scope and acceptance

The Office task list lets a user request deletion of a task explicitly. The UI must identify the task, ask for confirmation, prevent duplicate submissions, render API failures, and update the shared task list and current selection only after successful deletion. A task with an active lifecycle stage cannot be deleted; the UI explains the API's safe rejection. Never delete an existing user task during acceptance: create a disposable task for the delete check.

Add a separate LLM service panel that reports a real check against the configured host-side LLM CLI runner. It must have a manual recheck action and clearly distinguish success, unavailable/unconfigured, and failed checks. Provider API health is a different capability and must not be presented as CLI health. Unknown, absent, or failed responses must never render as healthy.

## Frontend scope and API handoff

- This change owns frontend presentation and repository/composable boundaries only. Backend API implementation is handled separately.
- The task repository calls `DELETE /tasks/:id`; the UI confirms first, prevents duplicate in-flight requests, preserves the task until success, then removes it from task/state collections. API errors stay visible through the existing Office error alert. The API must safely reject non-terminal tasks; the UI disables deletion for active tasks.
- The LLM Application use case chooses the first non-empty `models[*].modelId` for each catalog provider. The repository calls `POST /llm-cli/check` once per provider with `{ providerId, modelId }`, passing both catalog values unchanged. It validates that the response echoes the requested provider/model, `available` is boolean, `latencyMs` is finite and nonnegative, and optional `errorCode` is a string. Invalid responses, HTTP/auth/transport failures remain `尚未確認`; `available: true` means `已連線`, and `available: false` means `離線`, including CLI/model/timeout/network probe failures. Each provider appears as a compact card containing only service identity and current status; catalog data never implies CLI health. Checking is labeled `連線中`. Initial page load and manual refresh initiate probes; no polling is used. Anthropic uses the Simple Icons Anthropic SVG (CC0, https://github.com/simple-icons/simple-icons/blob/develop/icons/anthropic.svg); OpenAI uses `OpenAI-white-monoblossom.svg` from the official [OpenAI Logos 2025 ZIP](https://cdn.openai.com/brand/OpenAI-Logos-2025.zip); xAI uses its published logo asset from [Wikimedia Commons](https://commons.wikimedia.org/wiki/File:XAI-Logo.svg). Dark brand marks use a white circular background; the white OpenAI mark uses a black circular background for contrast. `agy` uses a neutral initial because its brand identity is not established by project data.
- Overall and per-provider CLI status must come from the authenticated `/llm-cli/check` minimal runner request. A provider HTTP health check or binary version check is not a substitute. Unknown, absent, failed HTTP responses, or unsupported data must never render as healthy.

## UI acceptance

Confirm dialog cancellation leaves task/list/selection unchanged; duplicate delete is blocked while pending; success removes the task and clears selection; 404/409/5xx/network errors remain visible and preserve consistent state. Verify distinct provider cards on desktop and mobile, partial provider failures, empty/unconfigured and unavailable states, collective recheck, direct refresh, and hydration. Record actual results and environmental limitations in the project docs.

## Frontend validation record

- Node 22.23.2 typecheck and production build passed in an isolated `/tmp` copy with its own `.nuxt`. Whole-repository ESLint and Prettier passed earlier; ESLint reports three existing `vue/no-v-html` warnings in `PixelOfficeMap.vue` and `WorkstationRoster.vue`. After the final logo contrast and click-handler edits, targeted component ESLint and Prettier passed; typecheck/build were not repeated for those final presentation/event-binding edits. This repository has no frontend unit-test script.
- The deployed authenticated route returned HTTP 200 with `available: false` and `UNKNOWN_MODEL` for a deliberately invalid provider/model. A live initial browser load rendered Anthropic/OpenAI as offline and agy/xAI as connected; these are observations at test time, not guaranteed current service health. Live manual refresh was not verified end to end.
- Deterministic Playwright route interception found and verified a manual-refresh correction: `@click="check"` passed a MouseEvent as the optional provider ID and sent no probes; `@click="check()"` explicitly invokes the collective check. Initial load and one manual refresh each issued exactly four mocked HTTP 200 POSTs with the catalog provider's first valid model. Alternating mocked availability updated all four cards independently. No real CLI/model probes were made by this test. Script: `/tmp/llm-mock-verify.cjs`; screenshots: `/tmp/llm-mock-desktop.png`, `/tmp/llm-mock-desktop-refresh.png`, `/tmp/llm-mock-mobile-375.png`.
- Desktop and 375px mobile screenshots were inspected. Compact cards display the brand, name and status; the OpenAI white mark is visible on its black background. All four mobile cards fit within the service panel. Existing overflow in the unrelated header/workstation layout remains outside this change.
- Did not issue a delete against existing user tasks. A disposable task could not be created with the connected service, so live task deletion pending/success/failure E2E remains unverified. Frontend deletion behavior from the prior commit is unchanged by this compact-card/API integration update.
