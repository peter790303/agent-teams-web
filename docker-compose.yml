services:
  agent-teams-web:
    build:
      context: .
      dockerfile: docker/22/Dockerfile
    ports:
      - '127.0.0.1:30679:3000'
    environment:
      NUXT_PUBLIC_API_BASE: ${NUXT_PUBLIC_API_BASE:-http://localhost:30678}
