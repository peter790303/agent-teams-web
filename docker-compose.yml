services:
  agent-teams-web:
    build:
      context: .
      dockerfile: docker/22/Dockerfile
    ports:
      - "30679:3000"
    environment:
      BACKEND_ORIGIN: http://host.docker.internal:30678
    extra_hosts:
      - "host.docker.internal:host-gateway"
